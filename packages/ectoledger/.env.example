# PostgreSQL connection URL for the EctoLedger.
# The agent state (event log and snapshots) lives here.
DATABASE_URL=postgres://ectoledger:ectoledger@localhost:5432/ectoledger

# Optional: run cognitive loop with `audit`. Ollama must be running on OLLAMA_BASE_URL.
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=mistral
# Comma-separated domains allowed for Tripwire http_get (e.g. api.example.com).
# AGENT_ALLOWED_DOMAINS=
# Set to require auth on the Observer dashboard (Bearer header or ?token=).
# OBSERVER_TOKEN=
# Max cognitive loop steps (default: 20).
# AGENT_MAX_STEPS=20
# Consecutive LLM errors before abort (default: 5).
# AGENT_LLM_ERROR_LIMIT=5
# Consecutive Guard denials before abort (default: 3).
# AGENT_GUARD_DENIAL_LIMIT=3
# Guard runs in a separate process (guard-worker). For production, set GUARD_REQUIRED=true
# and use a different model class than the primary (e.g. primary=anthropic/claude-3-5-sonnet, guard=ollama/phi3).
# GUARD_REQUIRED=false
# GUARD_LLM_BACKEND=ollama
# GUARD_LLM_MODEL=phi3
