services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ectoledger
      POSTGRES_PASSWORD: ectoledger
      POSTGRES_DB: ectoledger_test
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "127.0.0.1:5433:5432"
    tmpfs:
      - /var/lib/postgresql/data:uid=70,gid=70   # ephemeral; uid/gid 70 = postgres user in Alpine
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ectoledger -d ectoledger_test"]
      interval: 1s
      timeout: 5s
      retries: 20
