# Server Configuration

# Node Environment
NODE_ENV=development

# Port for the MCP server (defaults to 8080)
PORT=8080

# Logging Configuration  
# Log level: debug, info, warn, error (defaults to debug in dev, info in production)
# LOG_LEVEL=debug